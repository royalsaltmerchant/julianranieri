# Mayura English page

Do not edit this published copy directly unless there is an urgent production
fix. The source of truth for this page lives in:

`/Users/julianranieri/Desktop/Code_Base/mayura-english/site/`

After editing the source copy, sync it into this directory from the
`mayura-english` repository:

```bash
./dev/sync-site-to-julianranieri.sh
```

That script copies `site/` into this `mayura-en/` directory and excludes the
Mayura repo's standalone `site/CNAME` file and this local README.
